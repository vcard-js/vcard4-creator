import { describe, it } from 'vitest';
import { expect } from 'chai';
import {{{pascalCase property}}}Property, { type {{{pascalCase property}}}PropertyRestConfig } from '../../src/properties/{{{pascalCase property}}}Property.js';

describe('{{{pascalCase property}}}Property', () => {
    it('is a class', () => {
        expect({{{pascalCase property}}}Property).to.be.a('class');
    });

    describe('#toString()', () => {
        it('is a method', () => {
            expect({{{pascalCase property}}}Property.prototype.toString).to.be.a('function');
        });

        it('returns a string', () => {
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property('...');

            expect({{{camelCase property}}}.toString()).to.be.a('string');
        });

        it('returns the proper string format', () => {
            const value = '...';
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property(value);

            expect({{{camelCase property}}}.toString()).to.equal(`{{{upperKebabCase property}}}:${value}`);
        });

        it('correctly returns parameters', () => {
            const parameters = { pref: 1 };
            const value = '...';
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property(value, parameters);
            const actual = {{{camelCase property}}}.toString();
            const expected = `{{{upperKebabCase property}}};PREF=1:${value}`;

            expect(actual).to.equal(expected);
        });
    });

    describe('#valueOf()', () => {
        it('is a method', () => {
            expect({{{pascalCase property}}}Property.prototype.valueOf).to.be.a('function');
        });

        it('returns a string', () => {
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property('...');

            expect({{{camelCase property}}}.valueOf()).to.be.a('string');
        });

        it('returns the same value passed to it', () => {
            const value = '...';
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property(value);

            expect({{{camelCase property}}}.valueOf()).to.equal(value);
        });
    });

    describe('.from()', () => {
        it('is a static method', () => {
            expect({{{pascalCase property}}}Property.from).to.be.a('function');
        });

        it('returns an instance of `{{{pascalCase property}}}Property`', () => {
            const {{{camelCase property}}} = {{{pascalCase property}}}Property.from('...');

            expect({{{camelCase property}}} instanceof {{{pascalCase property}}}Property).to.equal(true);
        });

        it('returns an instance if provided one as an argument', () => {
            const {{{camelCase property}}} = new {{{pascalCase property}}}Property('...');

            expect({{{pascalCase property}}}Property.from({{{camelCase property}}})).to.equal({{{camelCase property}}});
        });

        it('creates an instance from a string value argument', () => {
            const {{{camelCase property}}} = {{{pascalCase property}}}Property.from('...');

            expect({{{camelCase property}}} instanceof {{{pascalCase property}}}Property).to.equal(true);
        });

        it('creates an instance from an array argument', () => {
            const value = '...';
            const config: {{{pascalCase property}}}PropertyRestConfig = [value, { type: 'home' }];
            const {{{camelCase property}}} = {{{pascalCase property}}}Property.from(config);

            expect({{{camelCase property}}} instanceof {{{pascalCase property}}}Property).to.equal(true);
        });
    });
});
