import {{{pascalCase property}}}Property, { type {{{pascalCase property}}}Config } from '../{{{pascalCase property}}}Property';

export default class {{{pascalCase property}}}PropertyArray extends Array {
    push(...items: {{{pascalCase property}}}Config[]): number {
        items.forEach(item => super.push({{{pascalCase property}}}Property.from(item)));

        return this.length;
    }
}
